dependencies {

    compile project(':structurizr-core')

    testCompile 'junit:junit:4.12'

}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDir 'test/unit'
        }
    }
}